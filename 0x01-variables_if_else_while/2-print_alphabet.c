#include <stdio.h>
#include <stdlib.h>

/**
 * main - print_alphabet.c
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char A;
A = 'a';
for (A = 'a'; A <= 'z'; A++)
{
putchar(A);
putchar("\n");
return (0);
}
