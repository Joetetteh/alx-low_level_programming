#include "main.h"

/**
 * c - function to to display lowercase 
 * _islower - function to check lowercase letters
 *
 * Return: lways (Success)
 */

int _islower(int c)
{

if (c >= 97 && c <= 122)
return (1);
else
return (0);
}
