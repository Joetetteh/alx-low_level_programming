#include "main.h"

/**
 * positive_or_negative - function to test for positve or negative num
 *
 * @i: parameter
 *
 * Return: Always (Success)
 */

void positive_or_negative(int i)
{
if (i >= 0)
printf("%d is positive\n", i);
else if (i < 0)
printf("%d is negative\n", i);
}
